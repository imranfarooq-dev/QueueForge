apiVersion: v1
kind: ConfigMap
metadata:
  name: queueforge-config
  namespace: queueforge
data:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: queueforge
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  KAFKA_BOOTSTRAP_SERVERS: kafka:9092
  SERVER_PORT: "8080"
